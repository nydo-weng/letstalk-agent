name = "english-speaking-agent"
main = "src/index.ts"
compatibility_date = "2024-01-01"
node_compat = true

# Worker 配置
[env.production]
name = "english-speaking-agent-prod"

[env.development]
name = "english-speaking-agent-dev"

# 环境变量（实际值通过 wrangler secret 设置）
# wrangler secret put OPENAI_API_KEY
